/**
* Takes in 3 numbers, returns their sum
 * @param {number} a
 * @param {number} b
 * @param {number} c
 * @returns {number}
 */

const solution = (a, b, c) => {
  return a+b+c;
}
console.log(solution(6,6,6));


module.exports = {
  solution,
};
